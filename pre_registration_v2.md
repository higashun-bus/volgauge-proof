# 事前登録 v2.1 — 確認的再現プロトコル

**対象**: VolGauge / ボラティリティ増分効果
**版**: v2.1（v2.0 から、独立イベント定義と検出力設計を改訂）
**作成日**: 2026-08-11
**ステータス**: ☑ §6 全項目解消済み・凍結可　☐ 確定・凍結

> **v2.0 からの変更点**
> 1. §2 独立イベントの定義を「方向別クラスタ化」から**「方向を区別しないクラスタ化」**に変更
> 2. §3 検出力設計を「点推定を真値と仮定」から**「CI下限を真値と仮定」**に変更（勝者の呪い対策）
> 3. §3 判定件数を 150件 → **267件**（`power_calc.py` で確定）
> 4. §5 診断1（ベースライン校正）を解決済みとして記録（b = 1.002）
> 5. §8 増分倍率を 1.50倍 → **1.312倍**（§2 仕様どおりの推定量）に訂正

---

## §0 位置づけの宣言 — 最初に読むこと

**この文書は「発見の事前登録」ではありません。**

2025–2026 の13ヶ月・1518件の解析で得られた結果は、**すべて探索的（exploratory）**です。
1h / 4h / 24h の3つの horizon を見て、EWMA と HAR-RV の2つのベースラインを見て、
その結果を見た後に「1h・HAR-RV比」を主張として選んでいます。事後的な条件選択であり、
どれだけ厳密に計算しても、それ自体が多重比較の産物である可能性を排除できません。

| 区分 | 対象 | 扱い |
|---|---|---|
| 探索的 | 13ヶ月・1518件の全結果 | 仮説の生成。証拠ではない |
| 確認的 | 本文書の凍結時刻以降に発生する新規イベントのみ | 仮説の検証。これのみが証拠 |

**過去13ヶ月のデータは本プロトコルの判定に一切使用しません。** 既に見てしまったデータから
ホールドアウトを切り出すことも禁止です（既に汚染されているため）。使えるのは前向きデータだけです。

この区別を LP・マーケ文面・Reddit 投稿のすべてで維持してください。

---

## §1 確認する仮説 — 1本のみ

> 出来高異常検知の直後1時間の実現ボラティリティは、
> HAR-RV による予測を条件づけてもなお、有意に高い。

これ以外は確認しません。4h も 24h も、EWMA も、銘柄別も、すべて探索的扱いのままです。

---

## §2 主要エンドポイントの完全定義

実装は `replication.py`。凍結後は編集しません。

| 項目 | 確定値 |
|---|---|
| horizon | 1時間（検知足の確定時刻を起点） |
| 実現ボラ | 検知足の次足から1時間分の対数リターン二乗和の平方根 |
| エントリー基準時刻 | 検知足の確定時刻。未確定足（`ohlcv[-1]`）は使用禁止 |
| ベースライン | HAR-RV：説明変数 = log RV(過去1h), log RV(過去24h), log RV(過去168h) |
| ベースライン学習 | 対照足（非検知足）のみで OLS 学習 |
| 主要回帰 | `log RV_fwd = a + b·log(HAR_pred) + c·D_signal` |
| 主要指標 | `exp(c)` |
| 標準誤差 | 日次クラスタ頑健（Liang-Zeger、小標本補正つき）と移動ブロック・ブートストラップ（ブロック3日・2000回）の**広い方**を採用 |
| 有意水準 | α = 0.05（両側） |
| 独立イベント | ①銘柄ごとに1時間窓の重複検知を1イベントに集約 → ②**方向を区別せず**、1時間窓で銘柄横断クラスタ化 |
| 対象銘柄 | BTC/USDT, ETH/USDT, SOL/USDT, DOGE/USDT。追加・削除禁止 |
| 検知パラメータ | `params_hash = dbf36471635b` に固定。変更した時点でカウントをリセット |

**判定基準**

- `exp(c)` の 95%CI 下限 > 1.0 → **再現成功**
- CI が 1.0 を含む → **再現失敗**

「惜しい」は失敗です。CI下限が 0.99 なら失敗として報告します。

### 独立イベント定義で「方向を区別しない」理由

**クラスタ化ルールは、シグナルではなく<u>アウトカムの依存構造</u>に合わせます。**

本プロトコルのアウトカムは**符号を持たない実現ボラティリティ**です。市場全体を動かすショックが
来れば、BTC も ETH も同時に高ボラになります。**片方の検知が BULLISH、もう片方が BEARISH と
タグ付けされていても、その後1時間の実現ボラは同じショックに駆動されており、独立ではありません。**
方向で分けて2イベントと数えると、標本の独立性を過大評価します。

（対比: 旧プロダクトの方向性検定では、方向で分けるのが**正しい**設計でした。BULLISH な BTC の
符号付きリターンと BEARISH な ETH の符号付きリターンは別の量であり、むしろ逆相関するためです。
**アウトカムが変われば、正しいクラスタ化ルールも変わります。**）

実測: 方向別だと 872 イベント、方向を区別しないと **843 イベント**。
推定値と CI は変わりません（SE は日次クラスタ頑健で算出しており、イベント定義に依存しないため）。
イベント数は判定の到達基準としてのみ使います。

---

## §3 サンプルサイズと停止規則

### ⚠️ 元の検出力計算は使えません（凍結前に判明）

元案は探索段階の `exp(c)=1.50, CI[1.35,1.62], n=807` から必要 n ≈ 83 件と算出していました。
**この 1.50倍は §2 の推定量が出した値ではありません。** `vol_baseline.py` は HAR ラグを
(24h, 168h, 720h) としていましたが、§2 は (1h, 24h, 168h) を指定しています。
**直前1時間のボラを説明変数に入れると増分が大きく削られます。**

§2 の推定量で過去13ヶ月を回した実測値（探索的）:

```
exp(c) = 1.312 倍   95% CI 1.215 – 1.416（cluster-robust / 広い方）
         独立イベント 843 件 / 検知 1501 件 / 385 日
         HAR校正係数 b = 1.002 [0.958, 1.047]
         陰性対照 0.988 倍 [0.930, 1.051] → clean
```

### 必要サンプル数は「CI下限」で設計する — 勝者の呪い

**点推定を真値と仮定して検出力を設計してはいけません。**

1.312倍 は、3つの horizon × 2つのベースライン × 複数の仕様を見た後に選ばれた推定値です。
選択された推定値は系統的に上振れします（winner's curse / regression to the mean）。
点推定で設計すると、再現テストは構造的に under-power になります。

そして under-power したテストが失敗したとき、
**「効果が無かった」のか「検出力が足りなかった」のか区別がつきません。**
この曖昧さが、本プロジェクトが積み上げてきた信頼を最も損ないます。

さらに重大な点として、§7 により再現失敗は
**LPトップへの掲載・課金者への通知・返金提示**という不可逆な対応を自動的に発動させます。
検出力の不足は、統計上の不備ではなく **自分のプロダクトを誤って停止させる事業リスク** です。

### 検出力の実測（`power_calc.py`）

```
c = ln(1.312) = 0.27155   SE = 0.03905   t = 6.953   発生率 66.7件/月
```

| n | 点推定1.312倍が真値 | **CI下限1.215倍が真値** | 偽陰性率 | 到達 |
|---:|---:|---:|---:|---:|
| 150 | 83.5% | **55.7%** | **44.3%** | 2.3ヶ月 |
| 170 | 87.7% | 61.0% | 39.0% | 2.6ヶ月 |
| 200 | 92.3% | 68.0% | 32.0% | 3.0ヶ月 |
| 250 | 96.6% | 77.5% | 22.5% | 3.8ヶ月 |
| **267** | **97.5%** | **80.1%** | **19.9%** | **4.0ヶ月** |
| 300 | 98.6% | 84.5% | 15.5% | 4.5ヶ月 |
| 400 | 99.8% | 93.0% | 7.0% | 6.0ヶ月 |

150件では、効果が本物（CI下限相当）でも **44.3% の確率で「再現失敗」が出ます。**
コイン投げで自社プロダクトを停止させることになります。

### 確定する運用

| 項目 | 確定値 |
|---|---|
| 判定を行う件数 | **独立イベント 267件（1回のみ）** |
| 判定回数 | 1回だけ。中間判定は行わない |
| 想定検出力 | 点推定が真値なら 97.5%、**CI下限が真値でも 80.1%** |
| 到達予測 | 66.7件/月 → **約4.0ヶ月**（2026年12月中旬） |

267 は `power_calc.py --mult 1.312 --lo 1.215 --hi 1.416 --n 843 --days 385` の出力です。
`replication.py` の `TARGET_EVENTS` と `FREEZE.json` の `target_events` を同値に固定しています。

### ライブ公開と逐次的覗き見の扱い — 最重要

LP でカウンタと推定値をライブ公開しますが、これは構造的に「毎日覗き見している」状態です。
有利な時点で止めれば、真の効果がゼロでも p<0.05 に到達する確率は約70%まで上がります。

1. **267件**に到達するまで、いかなる時点の値も判定に使用しない
2. ライブ表示の数値には常に **Interim — not used for the verdict** を併記する
3. 「良さそうだから今止める」「悪いのでもう少し続ける」はどちらも事前登録違反
4. 267件到達時点の値が、成功でも失敗でも唯一の結論
5. 267件に到達したら、その日のうちに1回だけ判定を実行し、結果を即日公開する

判定日を件数で固定しているのは、日付で固定すると発生率の変動で検出力が変わるためです。

---

## §4 陰性対照（プラセボ）

確認的判定と同時に実行し、両方が通って初めて成功とします。

1. 検知足をランダムな対照足に置き換えた疑似シグナルで、同じ回帰を実行
2. 判定は**両側**：`exp(c_placebo)` の 95%CI が 1.0 を含むこと
3. プラセボ実行時、本物の検知足が対照群に残っていないことを機械的に検証
4. **汚染検出のリグレッションテストを CI に組み込む**（`replication.py --selftest`）
5. 判定ロジックは `stats_core.control_verdict()` / `effect_verdict()` の**2関数に集約**。
   ゲートを追加するときは必ずこれを呼ぶ

### 実装済みの自己検証（全項目合格）

```
① 効果ゼロのデータで非有意になるか   0.992倍 [0.954, 1.034] → clean   ✅
② 本物の効果(1.5倍)を検出できるか    1.503倍 [1.445, 1.563] → effect  ✅
③ 汚染入力で陰性対照が失敗するか     0.944倍 [0.899, 0.981] → biased  ✅
④ 判定ヘルパーが両側で動くか         上振れ/下振れ/正常 すべて正       ✅
```

③が本プロトコルの要です。この汚染は**過去3回発生しています**（`volatility_study.py`、
`vol_baseline.py` の2モジュールと、判定関数の片側化）。注意力では防げないので、
「わざと汚染させたら必ず失敗する」ことをテストで固定しました。

---

## §5 事前指定する感度分析

主要判定は §2 のみ。以下は判定を変えません。結果は成否にかかわらず全て公開します。

### 必須（探索段階の異常の解明）

探索段階で、増分倍率（1.50倍）がベースラインなしの倍率（1.44倍）を上回る現象が
観測されました。**この異常は §2 の推定量では再現しません**（1.31倍 < 1.33倍）。
原因は HAR ラグ指定の違いであり、回帰の誤設定ではないことが以下で確認できます。

1. **ベースライン係数 b の校正（Mincer-Zarnowitz）** — 実測 **b = 1.002 [0.958, 1.047]**。
   1.0 を含んでおり、HAR_pred は完全に校正されています。
   **ダミーが拾っているのは校正ズレではありません。診断1は解決済み。**
2. `log(HAR_pred)` の十分位内で層別推定 — 確認的判定と同時に実施
3. 線形項をスプライン（または十分位ダミー）に置換して係数が残るか — 同上

### その他

- 検知足を学習に含めたベースライン
- EWMA(λ=0.94) をベースラインにした場合
- 銘柄別（4銘柄それぞれ）
- 上位・下位ボラ・レジーム別
- 移動ブロック・ブートストラップのブロック長を 1 / 3 / 6 日に変えた場合

---

## §6 未解決だった技術的論点 — すべて解消済み

### ☑ 1. 独立イベント数の不整合

4h が 503 と 471、24h が 264 と 247 と食い違っていた件。**原因は HAR-RV の
ウォームアップ落ちで確定しました。**

```
経路1（volatility_details.csv 由来）  1h 1513→853 / 4h 1516→503 / 24h 1511→264
経路2（vol_baseline の HAR 経路）     検知1521 → ウォームアップ落ち80 / 末尾落ち0〜5
                                      → 採用1441/1441/1436 → 809/471/247
```

`vol_baseline.py` は最大ラグ 720本のため先頭80件（各銘柄の最初期）が脱落していました。
**v2.0 の HAR は最大ラグ 168本なので、ウォームアップ落ちは大幅に減ります**
（§2 の推定量では検知 1501 件が採用され、843 独立イベント）。
確認的判定では `replication.py` の1経路しか使わないため、この不整合は構造的に発生しません。

### ☑ 2. 自己検証の移植

`stats_core.py` / `replication.py` は仕様から再実装したものであり、元実装の自己検証結果は
この実装のものではありませんでした。§4 の4項目を `replication.py --selftest` として移植し、
全項目の合格を確認済みです。CI に組み込んでください。

### ☑ 3. params_hash の確定

```
params_hash = dbf36471635b
  lookback 20 / method ratio / threshold 3.0 / ema_period 200
  rsi_period 14 / rsi_basis prev / timeframe 1h / exchange bybit
  symbols BTC/USDT, DOGE/USDT, ETH/USDT, SOL/USDT
```

---

## §7 報告義務 — 失敗時の対応を先に決める

**再現失敗（CI が 1.0 を含む）の場合:**

1. 失敗を LP のトップに掲載する。**探索的結果を代わりに掲載しない**
2. 主要指標の表示を停止し、`vol_record.py` のゲートを `replication_failed` に遷移させる
3. 既存の課金者に対し、**こちらから**再現失敗を通知する。問い合わせを待たない
4. 通知と同時に、**当月分の返金または解約の選択肢を提示する**
5. プロダクトの主張を「ボラティリティ・レジームの可視化ツール」に再定義するか、
   サービスを停止するかを判断する

4 は事前に確定させます。失敗が分かってから返金方針を考えると、必ず自分に有利な方に歪みます。

**再現成功の場合も、以下を必ず併記します:**

- 探索的（13ヶ月）と確認的（前向き267件）の両方の数字と、その区別
- 独立イベント数（生の検知件数ではない）
- 95% 信頼区間
- HAR-RV ベースラインに対する増分であること
- 観測期間の市況（上昇／下落／レンジ）
- 「方向は予測できなかった」negative result への導線
- §5 の感度分析の全結果（不利なものを含む）

---

## §8 LP 表示仕様

### ヒーロー数値

**1.33×**（補正後の素の倍率、95% CI 1.22–1.45、807 independent events）

増分倍率をヒーローにしません。理由は3つ:

1. 厳密化によって他の全指標が下がった中で、見出しだけが上がる構図になる
2. 機構が未解明（§5 診断2・3が未実施）
3. **そもそも 1.50倍は §2 の推定量の値ではありません。** §2 の仕様で計算すると 1.31倍で、
   素の 1.33倍とほぼ同じです。1.50倍を看板にすると、確認的再現の結果が出た瞬間に
   「下方修正した」と読まれます

確認的再現が成功し、かつ §5 の診断2・3が通った時点で、ヒーローの引き上げを検討します。
先に低く出して後から上げるのは「追加検証に通った」と読まれ、先に高く出して後から下げるのは
「批判されて撤退した」と読まれます。同じ最終値でも信頼の増減が逆になります。

### 併記する表

| 指標 | 値 | 区分 |
|---|---|---|
| 素の倍率（補正後） | 1.33× [1.22–1.45] | Exploratory |
| HAR-RV 比 増分（vol_baseline 仕様） | 1.50× [1.35–1.62] ※機構未解明 | Exploratory |
| HAR-RV 比 増分（§2 事前登録仕様） | 1.31× [1.22–1.42] | Exploratory |
| 4h 増分 | 1.28× [1.20–1.36] | Exploratory |
| 24h | 1.03× [0.98–1.08], p=0.684 — 補正すると何も残らない | Exploratory |
| 確認的再現 | 進行中 | Confirmatory |

**24h の行は消さないでください。** 「効かないものを効かないと書いている」ことが、
残りの数字の信頼性を担保します。

### ライブ再現カウンタ

```
──────────────────────────────────────────────
  PRE-REGISTERED OUT-OF-SAMPLE REPLICATION

  Everything above was found in 13 months of historical
  data. That is a hypothesis, not proof.

  We locked the test criteria on <FREEZE_DATE>
  (SHA-256: <HASH>) and are now testing them on data that
  did not exist when we locked them.

      Progress   ██████░░░░░░░░░░░░░░  23 / 267 events
      Current    1.28×   ← Interim. Not used for the verdict.
      Verdict at 267 events. Expected <DATE>.

  We will publish the result whether it holds or not.
  If it fails, this page will say so, and we will contact
  every subscriber directly and offer a refund.

  Read the pre-registration →
──────────────────────────────────────────────
```

`Current` には必ず **Interim — not used for the verdict** を併記してください。

---

## §9 記入欄

ハッシュはこの文書内に書きません（自己参照になるため）。
`freeze_preregistration.py --freeze` が `FREEZE.json` に生成します。

| 項目 | 値 |
|---|---|
| 凍結時刻（UTC） | `______________________` |
| 凍結時点の params_hash | `dbf36471635b` |
| カウント開始時刻（UTC）※凍結時刻と同一 | `______________________` |
| 判定予定件数 | **267** 独立イベント |

### 凍結手順

```bash
git init                                  # 未初期化なら凍結コミットの前に必ず
python replication.py --selftest          # 全項目合格を確認してから凍結する
python freeze_preregistration.py --files pre_registration_v2.md stats_core.py replication.py power_calc.py
python freeze_preregistration.py --freeze --files pre_registration_v2.md stats_core.py replication.py power_calc.py
git add -A && git commit -m "Pre-registration v2.1 frozen — confirmatory replication starts now"
git log -1 --format="%H %cI"
```

凍結後、`FREEZE.json` の SHA-256 を LP と X に公開してください。
OpenTimestamps（無料）でビットコインのブロックチェーンに刻んでおくと、
「凍結時刻を後から偽装していない」ことまで証明できます。

改変チェックはいつでも `python freeze_preregistration.py --verify` で行えます。

**凍結後、この文書と `stats_core.py` / `replication.py` / `power_calc.py` を編集しないでください。**
編集が必要になった場合は v3.0 として新規作成し、カウントをリセットして、両方を残します。
